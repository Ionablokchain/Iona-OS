[package]
name    = "iona-wasm-counter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name       = "iona_wasm_counter"

[profile.release]
opt-level = "s"
strip     = true
lto       = true
panic     = "abort"
